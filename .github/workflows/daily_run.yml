name: Daily Portfolio Run (disabled — runs via Colab)

on:
  workflow_dispatch:

jobs:
  run-portfolio:
    runs-on: ubuntu-latest
    steps:
      - name: Pipeline runs via Google Colab
        run: |
          echo "This pipeline runs via Google Colab + GitHub API push."
          echo "See the Colab notebook for the active pipeline."
          echo "Dashboard: https://bromomo-git.github.io/SP500-Portfolio-Optimizer/"
